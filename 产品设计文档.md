# 网页智能填充助手｜产品设计文档 v0.1

## 1. 产品定义

面向保研申请等需要在多个网站重复填写个人资料的场景，提供 Chrome 浏览器插件。用户只维护一份结构化个人资料；进入报名网站后手动启动插件，系统扫描当前页面并尽可能自动完成可确定字段，用户检查后自行提交。

长期定位不局限于“保研填表”，而是逐步形成可复用的个人履历数据层，未来可扩展到秋招、奖学金申请、项目申报等重复填表场景。

## 2. 核心问题

当前海投流程存在三个主要摩擦：

- 同一姓名、学校、专业、成绩、奖项、科研经历等信息需要在不同系统反复录入。
- 不同网站对同一语义使用不同字段名称、控件和格式，例如“籍贯/生源地”“研究计划/科研计划”，以及文本框、下拉框、单选框等不同控件。
- 少量字段不是固定事实，而是需要针对学校、专业和上下文临时撰写，例如个人评价、研究计划等。

产品目标不是追求 100% 无人值守，而是优先自动完成大部分重复、确定性的输入，把剩余少量特殊字段交给用户或 AI。

## 3. 核心设计原则

### 3.1 Single Source of Truth

用户在插件内拥有固定的“个人资料中心”，所有自动填充都读取这里的数据。用户更新奖项、科研经历等信息后，之后的填充自动使用最新版，不依赖外部备忘录反复复制。

### 3.2 Rule First, AI on Demand

默认不让大模型参与每一个字段。优先使用确定性规则完成识别和填充，以降低延迟、成本和错误率。

建议决策链：DOM/标签解析 → 字段词典与别名匹配 → 格式转换与控件适配 → 必要时语义匹配 → 必要时 AI 内容生成 → 无法确定则留给用户。

### 3.3 固定 Schema + 可扩展字段

系统维护高频核心 Schema，降低普通用户的配置门槛；同时允许用户在对象下新增自定义字段，避免产品被预定义字段锁死。

例如“科研经历”可以包含项目名称、时间、导师、职责、成果等标准字段，同时允许增加“科研计划”等扩展字段。

### 3.4 支持对象、数组，而非纯 Key-Value

个人履历天然包含一对多结构。奖项、教育经历、科研经历、论文、实习等均应作为对象数组保存，而不是压平成一组字符串。

概念结构示例：

```json
{
  "basic_profile": {},
  "education": [],
  "awards": [],
  "research": [],
  "publications": [],
  "internships": [],
  "custom_fields": {}
}
```

### 3.5 用户始终拥有最终控制权

插件由用户手动启动，不在页面打开后自动操作；插件不负责最终提交。低置信度、生成式内容和无法确认的字段应明确提示用户检查。

## 4. 数据策略

MVP 使用 `chrome.storage.local` 保存结构化资料，暂不建设账号体系、云数据库和跨设备同步。这样可以降低后端开发量，也减少个人敏感资料上传云端的必要性。

首次录入以插件内表单为主。未来可支持上传简历，由 AI 抽取为结构化字段，但抽取结果必须先展示给用户确认，再写入正式资料库。后续维护仍以结构化资料中心为准。

## 5. 字段体系的形成方式

正式开发前需要完成字段调研。收集不同高校报名系统的表单页面，记录：字段名称、所属模块、是否必填、控件类型、候选项、格式要求、同义字段、出现频率及特殊校验。

调研结果用于决定：核心 Schema 包含哪些字段；字段别名词典如何构建；哪些格式转换可以规则化；哪些字段必须进入 AI 或人工流程。

产品不应在当前阶段凭经验一次性拍定完整字段清单，而应由真实网页样本驱动 Schema。

## 6. 填充引擎

页面扫描器读取当前 DOM 中可填写控件及其上下文，包括 label、placeholder、name、id、附近文字、选项列表和控件类型。

规则引擎把网页字段映射到内部标准字段，例如“联系电话/手机号/手机号码”映射到 `basic_profile.phone`。匹配成功后，控件适配层负责把内部值转换为网页需要的操作，例如输入文本、选择下拉选项、勾选单选框。

无法高置信匹配时才进入智能层。智能层分两类：一是判断网页字段与已有资料字段之间的语义对应关系；二是对于个人评价、研究计划等没有固定 value 的开放文本，根据用户履历和当前申请上下文辅助生成内容。

## 7. 交互模型

主入口采用 Chrome 插件。用户在报名页面点击插件并主动执行“开始填充”。系统扫描整个页面，一次性填写所有能够可靠确定的字段，不设计“先填哪些、后填哪些”的优先级流程。

建议填充后给出结果摘要，例如“已填充 26 项 / 3 项需确认 / 4 项暂无资料”，并允许定位到未完成字段。

## 8. 履历关联能力：二期方向

长期可在教育、科研、论文、奖项、实习等对象之间建立关系。例如某篇论文关联某段科研经历和导师，某项奖项关联某个项目。

这使数据层从“字段仓库”升级为“履历关系图”。未来生成针对某个专业的个人陈述或简历时，可以按相关性抽取相互关联的经历，而不是简单拼接所有信息。

该能力有价值，但不进入第一版自动填表 MVP，以避免过早扩大范围。

## 9. 产品边界

第一阶段不追求：所有高校网站 100% 兼容、验证码绕过、自动提交、复杂流程自动导航、云端账户同步、自动从互联网修改用户资料、完整履历知识图谱。

第一阶段只验证一件事：一份用户资料能否在大量异构报名表单中，稳定、快速、可控地复用。