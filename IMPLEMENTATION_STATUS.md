# Career OS 0.5.0 Demo 实现状态

0.5 新增 Electron 跨平台桌面外壳，复用既有工作台和只读插件。Windows / macOS 启动脚本自动创建独立 `.demo-venv`、安装锁定依赖并构建插件；无需 Swift。提供环境检查、中文 PDF 编译检查、本地填表演练、白名单发布 ZIP、README 完整 SOP 和 GitHub 发布说明。

新增服务在父进程 stdin 管道关闭时退出，避免 Windows 使用 POSIX `os.kill(pid, 0)` 的不兼容行为。Windows 数据位于 `%LOCALAPPDATA%/Career OS`，Mac 沿用 Application Support 目录。

GitHub Actions 包含 Windows / macOS 安装、构建、回归、Electron 启动和退出检查，尚未上传执行，不能当作平台验收证据。Mac Electron 已通过真实页面就绪、空库和只读配对检查；Windows / Intel Mac 真机、Chrome 安装态及原生对话框操作仍待验收。

## 0.5 发布包验证

- 在项目目录以外的全新临时目录解压发布 ZIP，执行 `npm ci`、`npm run demo:setup` 成功，包含首次生成 WXT 配置、Python 依赖安装及插件构建。
- TypeScript 检查通过；38 项前端、20 项 Python 测试通过。包含演练表单的学历／学位区分、Electron 桥接和服务生命周期。
- 发布包真实 Electron 启动通过：页面就绪、空资料库、配对后允许读取且拒绝写入、退出后服务停止。
- XeLaTeX 真实材料 PDF 集成及 `doctor -- --pdf` 中文编译通过；使用的是本机已安装的 TeX，并非发布包自带。
- Windows x64 / Python 3.12 的主要二进制依赖可下载，已提供两系统 CI；这不替代 Windows 真机验证。
- ZIP 文件清单与 SHA256 已检查，排除个人目录与凭据；源码 Demo 仍需用户安装 Node、Python 和 PDF 依赖。

## 本次收敛

| 内容 | 实现 |
| --- | --- |
| 唯一维护入口 | Electron 桌面应用；浏览器根地址仅提示使用桌面应用 |
| 插件 | 只读配对、场景选择、扫描、映射预览、定位、确认填写 |
| 权限约束 | 插件凭据只允许读取填写资料和暂存迁移候选；服务器拒绝资料／事实写入、材料访问和完整备份 |
| 桌面启动 | Demo 先安装 Node / Python，再运行启动脚本；自动启动、认证，无需另启服务或选择密钥 |
| 生命周期 | 正常退出及父进程管道断开时停止服务，保留用户资料 |
| 数据位置 | Mac Application Support、Windows Local AppData，与项目源码分离 |
| 旧浏览器资料 | 配对后暂存候选；桌面确认后导入空库，不允许插件直接改 SSoT |
| 编辑体验 | 个人、经历及补充证据位于同一维护区；标准枚举与自定义旧值可编辑 |
| 文件操作 | 桌面原生选择与保存对话框、数据目录打开；PDF、LaTeX、JSON 导出 |
| 历史 | 保留 0.3 的事实版本、材料快照与修订历史 |

## 已有 0.4 验证记录（原生 Mac 外壳）

- TypeScript 检查，插件生产构建；桌面 Swift 编译、完整打包及本地签名验证。
- 前端回归覆盖填写规则、只读客户端、离线拒绝旧副本、迁移候选暂存、桌面自动读取、保存、删除最后一条记录及无重复维护入口。
- Python 回归覆盖原有材料链路、配对单次使用／尝试上限、只读权限、导入需确认、迁移不覆盖、真实 PDF 编译。
- 打包后的原生应用已启动内置服务，并通过页面就绪回执确认 WebKit 成功加载资料；在隔离测试目录验证实际打包服务的保存、PDF 编译、配对与写入拒绝。
- 原生窗口截图与真实点击验收受 CUA 界面工具连续超时阻塞，未宣称已完成原生视觉／对话框手动验收。页面逻辑以交互回归测试补充验证。
- Chrome 扩展管理页／内部页此前被自动审批限制；安装态重新加载及配对由用户完成，未绕过限制。

## 保留边界

本版桌面仅迁入 JSON 导入能力；旧插件的 PDF/DOCX 初始化、飞书导入和模型字段兜底已移出插件，但尚未迁入桌面。模型材料改写仍在桌面可选使用。旧源代码保存在 archive，不再打包。

尚无 Windows 正式安装程序、Apple 公证分发、自动更新、完整备份一键恢复、OCR、能力图谱、Career Inbox 和完整面试助手。PDF 仍需用户安装 TeX Live / MacTeX。网站跨域 iframe、封闭 Shadow DOM 及最终提交仍沿用既有边界。
