# 网页智能填充助手｜PRD v0.1

## 1. 文档信息

产品形态：Chrome Extension

首要场景：保研/高校项目申请网站重复填表

版本目标：MVP v0.1

核心成功标准：在目标样本网站中，显著减少用户重复录入；以“可可靠自动填充字段覆盖率”为核心指标，阶段目标约 80%，具体基线待字段调研后确定。

## 2. 目标用户与典型场景

核心用户是需要同时申请多所学校、夏令营、预推免、项目或其他高校系统的学生。典型流程是：用户已经拥有稳定的个人履历信息，但必须在不同网站反复填写相同或高度相似的数据。

核心用户故事：作为申请者，我希望只维护一次个人资料，在进入新的报名页面后点击一次插件，就能自动填写绝大多数已有信息，只处理剩余的特殊问题。

## 3. MVP 用户流程

首次使用：安装插件 → 打开“个人资料” → 按固定表单填写信息 → 可新增多条教育/奖项/科研等记录 → 保存到本地。

日常维护：打开“个人资料” → 修改字段或新增对象 → 保存 → 后续所有网站使用最新资料。

报名填充：进入报名网站 → 点击插件 → 点击“开始填充” → 扫描页面 → 规则匹配 → 自动填写 → 标记未匹配/不确定字段 → 用户检查与补充 → 用户自行提交。

## 4. 功能需求

### F1. 个人资料中心｜P0

插件提供固定资料管理页面，而不是要求用户每次填表前重新输入。

要求：支持查看、编辑、保存；支持基础单值字段；支持教育经历、奖项、科研经历、论文、实习等多条记录；支持对象内扩展自定义字段；刷新或关闭浏览器后数据仍存在。

存储：MVP 使用 `chrome.storage.local`。

验收：修改任一资料后重新打开插件，数据保持一致；新增数组记录后能够被填充引擎读取。

### F2. Schema 与扩展字段｜P0

系统提供预定义核心字段，用户不需要自行设计数据库；每个主要对象允许添加“字段名 + 字段值”的扩展字段。

自定义字段不能保证仅靠关键词规则识别。匹配路径为：先进入字段索引；网页出现相同/别名关键词时规则匹配；无法确定时可交由语义匹配模块判断。

### F3. 页面表单扫描｜P0

Content Script 扫描当前页面的可填写控件，包括至少：文本输入框、数字输入框、textarea、select、radio、checkbox，以及常见可编辑组件。

每个控件提取上下文特征：label、placeholder、name、id、邻近文本、控件类型、候选 options、required 状态等。

验收：在测试页面中能够输出结构化的“网页字段列表”。

### F4. 规则匹配引擎｜P0

维护标准字段及别名词典，例如“手机/手机号/联系电话”对应同一内部字段。综合 label、placeholder、name、id 等信号进行匹配，而非只依赖单一文本。

规则引擎输出至少包含：目标 DOM 元素、内部字段路径、匹配置信度、匹配依据。

应支持基础格式归一化，例如日期格式、文本清洗以及下拉选项 value 与显示文本之间的映射。

### F5. 控件填充｜P0

匹配成功后根据控件类型执行相应操作。对于文本框写入文本；对于 select 选择最接近的选项；对于 radio/checkbox 根据选项语义选择。

填充后需要触发网站常用的 input/change 等事件，避免“视觉上填了但前端框架没有记录”的问题。

插件不得自动点击最终“提交/确认报名”等不可逆按钮。

### F6. 填充结果反馈｜P0

执行结束后显示三类结果：已可靠填充、需要确认、未找到资料/无法处理。用户应能快速定位未完成字段。

建议显示统计，例如：`已填充 26 / 待确认 3 / 未处理 4`。

### F7. AI 语义匹配｜P1

仅当规则引擎无法达到置信阈值时调用。输入应尽量只包含当前网页字段的上下文和候选用户字段，而不是无差别发送整个个人资料库。

用途示例：“科研计划”与用户扩展字段“研究计划”的语义映射。

AI 输出映射建议及置信度；低置信度结果不自动填写。

### F8. AI 开放文本生成｜P1

对于个人评价、研究计划等需要针对当前学校/专业定制的开放题，允许用户主动选择“AI 辅助”。系统基于用户已确认的履历资料和页面题目生成草稿。

生成内容必须与固定事实字段区别展示，并要求用户确认；MVP 不允许生成后直接自动提交。

### F9. 简历初始化｜P1

首次使用可选择上传简历，通过解析/AI 提取候选资料。系统必须进入“预览并确认”步骤，用户确认后才写入正式资料库。后续资料更新不依赖自动解析。

## 5. 建议系统架构

`Extension UI`：Popup/Side Panel + 个人资料管理页。

`Storage Layer`：封装 `chrome.storage.local`，向上提供统一 CRUD，避免业务代码直接散落调用 Chrome API。

`Content Script`：读取当前页面 DOM、执行控件操作。

`DOM Parser`：把异构网页控件标准化为内部 Field Representation。

`Rule Engine`：字段别名、正则、打分和格式转换。

`AI Gateway`：仅处理语义消歧、简历解析、开放文本生成；与规则引擎解耦。

`Fill Orchestrator`：组织扫描 → 匹配 → 填充 → 反馈全过程。

## 6. 建议数据模型

顶层至少预留：`basicProfile`、`education[]`、`awards[]`、`research[]`、`publications[]`、`internships[]`、`customFields`、`schemaVersion`。

每个对象建议拥有稳定内部 ID，避免数组排序变化后关系失效。扩展字段保存用户可读 label 与 value；未来如建设履历关系图，可在对象间增加 relation IDs，而无需推翻现有结构。

完整字段清单暂不在本 PRD 锁死。下一阶段应通过真实学校表单样本统计后确定 v1 Schema。

## 7. 非功能需求

隐私：MVP 个人资料默认保存在本地；涉及 AI 时明确区分哪些数据需要发送到模型服务。

可靠性：无法确定时宁可不填，也不应把高风险事实字段错误填入。

性能：纯规则路径应尽量做到用户点击后快速完成，不应因为 AI 请求阻塞所有普通字段。

可解释性：至少在调试模式保留“网页字段 → 内部字段 → 匹配依据”，便于扩充词典和定位兼容性问题。

可扩展性：Schema、别名词典、控件适配器和 AI 模块应解耦，方便后续扩展秋招等场景。

## 8. MVP 明确不做

不做账号/云同步；不做跨设备资料同步；不自动提交申请；不处理验证码绕过；不保证所有自定义前端组件；不自动修改用户正式资料；不建设完整知识图谱；不做无人值守的多网站批量申请机器人。

## 9. 数据调研任务：开发前置

建立“学校表单字段样本库”。每个网页至少采集：学校/项目、页面模块、字段原文、控件类型、required、options、格式限制、同义表达、是否可由固定资料回答、是否需要上下文生成。

统计字段出现频率并形成 Pareto 分布，优先把覆盖率最高的字段纳入核心 Schema 和规则词典。这一步直接决定 MVP 的实际覆盖率，应先于大规模规则开发。

## 10. 版本路线

V0：完成表单样本采集工具/流程、字段统计与核心 Schema。

V1 MVP：本地资料中心 + DOM 扫描 + 高频规则匹配 + 常见控件填充 + 结果反馈。

V1.5：自定义字段语义匹配、简历初始化、开放文本 AI 辅助。

V2：规则学习/站点适配沉淀、复杂组件兼容、可选云同步。

V3：履历对象关联与关系图，将同一份个人履历数据用于保研、秋招、材料生成等场景。

## 11. 核心指标

自动填充覆盖率 = 成功自动填充的可回答字段数 / 页面中可由用户资料回答的字段数。

准确率 = 用户无需修改的自动填充字段数 / 自动填充字段数。

人工补充字段数：一次申请中用户仍需手填的字段数量。

单页面节省时间：手工完整填写耗时与插件辅助耗时之差。

AI 调用率：需要 AI 处理的字段占全部字段比例；该指标用于验证“Rule First”的设计是否成立。

## 12. MVP 验收建议

选择一批结构和技术栈不同的真实高校报名页面作为固定测试集。首先人工标注每个字段的标准含义和正确答案，再测试插件。

首版不应只看覆盖率。建议同时设置准确率门槛，因为“填错 80%”比“可靠填对 60%”更差。最终目标是在高准确率前提下逐步逼近约 80% 的可回答字段覆盖率。