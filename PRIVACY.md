# 隐私说明 · 0.5 跨平台 Demo

职业资料的唯一活动数据源是本机用户数据目录中的 `career.db`。macOS 为 `~/Library/Application Support/Career OS/`，Windows 为 `%LOCALAPPDATA%\Career OS\`。附件、PDF、来源快照及历史在同一目录。旧项目库和浏览器原资料如存在则保留为迁移备份，不参与日常填写。发布包不携带作者的资料、登录状态或凭据，新用户自行建立空库。

资料只在桌面应用编辑。Chrome 插件不包含资料编辑、简历导入、模型写作、飞书导入或岗位管理入口；仅保存配对凭据、场景偏好和原有迁移备份。扫描时读取桌面当前资料，确认前检查资料是否变化，离线时拒绝使用旧副本。

服务仅监听本机环回地址。桌面拥有维护权限，插件使用独立的只读凭据；服务器会拒绝插件修改资料与事实、读取材料和完整备份。配对码有效期五分钟、只能使用一次，连续错误尝试会使其失效。旧插件可暂存待迁移候选，必须在桌面确认后才写入空资料库。

页面请求中的资料响应不缓存。桌面 Electron（旧 Mac 外壳为 WebKit）持久化场景等界面状态，维护密钥仅注入受信任本地工作台内存。macOS 配置设置用户读写权限，Windows 继承用户数据目录 ACL。SQLite 未加密，有本机文件权限的程序仍可能读取。关闭应用停止服务；移除插件或项目不会自动删除个人数据。

默认没有开发者运营的资料上传或遥测服务。只有桌面材料页中用户主动填写模型接口并点击授权后，才发送选中的来源事实和岗位要求至该 HTTPS 接口。自由文本可能含个人信息，用户应在授权前核对；密钥仅用于单次请求，模型结果另存为草稿，不修改事实库。

原生文件选择、JSON／PDF／LaTeX 保存均由用户在本机完成。应用不会自动上传 PDF 或提交申请。完整备份和历史可能包含敏感资料，应由用户妥善保存。应用日志仅用于启动及页面就绪／错误诊断，不主动记录资料正文或密钥。
